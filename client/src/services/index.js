import { authInstance, normalInstance, requestAuth } from './instances'
import { ENDPOINTS, BASE_URL, DEFAULT_HEADERS } from './config'

export {
  authInstance,
  requestAuth,
  normalInstance,
  ENDPOINTS, 
  BASE_URL,
  DEFAULT_HEADERS
}